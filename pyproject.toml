[build-system]
requires = ["setuptools >= 77.0.3", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "modexiaagentpay"
version = "0.8.0"
description = "Modexia AgentPay — Python SDK for agent wallets & payments (USDC)"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
license-files = ["LICENSE"]
keywords = ["modexia", "agentpay", "sdk", "payments", "usdc"]
authors = [
	{ name = "Modexia", email = "modaniels@modexia.software" }
]
classifiers = [
	"Development Status :: 4 - Beta",
	"Intended Audience :: Developers",
	"Programming Language :: Python :: 3",
	"Programming Language :: Python :: 3 :: Only",
	"Programming Language :: Python :: 3.8",
	"Programming Language :: Python :: 3.9",
	"Programming Language :: Python :: 3.10",
	"Programming Language :: Python :: 3.11",
	"Operating System :: OS Independent",
	"Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = ["requests>=2.31.0", "httpx>=0.27.0"]

[project.urls]
Homepage = "https://modexia.software"
Repository = "https://github.com/Modexia/ModexiaAgentpay"
Documentation = "https://modexia.software/docs"
Changelog = "https://github.com/Modexia/ModexiaAgentpay/releases"

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.23.0", "pytest-httpx>=0.30.0", "build", "twine", "requests-mock"]

[tool.setuptools.packages.find]
where = ["src"]